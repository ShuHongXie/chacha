#include <stdio.h>

int main()
{
  // 1.编程实现:输人一个字符,如果它是一个小写字母,则把它变成大写字母;如果它是一个大写字母,则把它变成小写字母;其他字符不变。
  // 2.编程实现从键盘输入3个整数并按从小到大的顺序进行输出。
  // 3,编程实现输人一个100以内的实数x,可以在一行中按“f(x)=result”的格式输出其中x与result都保留两位小数。x与result都保留两位小数
  // f(x) = 1/x, x≠0； f(x) = 0; x=0;
  return 0;
}