#include <stdio.h>
#include <string.h>

enum Color
{
  BLUE, // 注意这里用,进行分隔，而不是;
  RED,
  PINK,
  BLACK,
  WHITE

};
int main()
{
  // enum Color color = BLUE;
  // printf("%d\n", color);
  // printf("%d\n", RED);
  // printf("%d\n", PINK);
  // printf("%d\n", sizeof(color));
  // printf("%d\n", sizeof(enum Color));
  // char str[] = "\xab\12\001\00\t";
  // int a;
  // float c;

  // char s1[20] = "he\0llo", s2[20] = "abcdef";
  // strcat(s1, s2 + 3);
  // printf("%c", s1[3]);
  long num = 1, i;
  printf("%d", -1 && -1);
  return 0;
}