#include <stdio.h>
#include <stdlib.h>

// ¸´ÖÆ
void main()
{
  // FILE *read, *write;
  // read = fopen("./t1.txt", "r");
  // write = fopen("./t2.txt", "w");
  // char ch = fgetc(read);
  // while (!feof(read))
  // {
  //   fputc(ch, write);
  //   ch = fgetc(read);
  // };
  // fclose(read);
  // fclose(write);

  float f = 1.4565;
  printf("%.3f", f);
  // char ch[20] = "abcdefghi";
  // char *p = ch;
  // p++;
  // *p = '4';
  // printf("%s", p);
}